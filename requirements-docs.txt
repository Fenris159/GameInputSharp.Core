# MkDocs and extensions for building the offline documentation site.
# Install: pip install -r requirements-docs.txt
# Build: mkdocs build
# Serve (optional): mkdocs serve

mkdocs>=1.5.0
mkdocs-material>=9.0.0
pymdown-extensions>=10.0
